[package]
name = "testing"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
snapbox = "0.6"
tempfile = "3.20"
toml = "0.8"

[lints]
workspace = true
