[package]
name = "ci"
version = "0.1.0"
edition = "2024"
publish = false

[dev-dependencies]
assert_cmd = "2.0"
ctor = "0.5"
regex = "1.11"
serde_json = "1.0"
similar-asserts = "1.7"
tempfile = "3.23"

testing = { path = "../testing" }

[lints]
workspace = true
