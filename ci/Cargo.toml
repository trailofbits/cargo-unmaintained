[package]
name = "ci"
version = "0.1.0"
edition = "2024"
publish = false

[dev-dependencies]
assert_cmd = "2.0"
ctor = "0.4"
regex = "1.11"
similar-asserts = "1.7"
tempfile = "3.20"

testing = { path = "../testing" }

[lints]
workspace = true
